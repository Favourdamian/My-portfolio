# Portfolio Starter

Minimal Vite + React starter for a personal portfolio.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Build and preview:

```bash
npm run build
npm run preview
```

Customize components in `src/components`.
